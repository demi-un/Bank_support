from __future__ import annotations

from datetime import datetime
from typing import List, Dict, Tuple

from langchain.schema import HumanMessage, SystemMessage
from langchain_community.chat_models.gigachat import GigaChat

from .config import API_KEY


# ================== ПРОМПТЫ ==================

CLASSIFIER_PROMPT = SystemMessage(content="""
Ты выступаешь в роли классификатора обращений клиентов банка.
Ответь ТОЛЬКО одним словом:
BANK — если вопрос относится к банковским продуктам, сервисам или операциям.
NON_BANK — если вопрос не связан с банковскими услугами.
""")

ANSWER_PROMPT = SystemMessage(content="""
Ты виртуальный консультант Сбербанка.

СТРОГИЕ ПРАВИЛА:
- Используй ТОЛЬКО информацию из предоставленного контекста.
- НЕ добавляй информацию, которой нет в контексте.
- НЕ придумывай детали, цифры, условия или процедуры.
- НЕ добавляй общие фразы типа "обратитесь в отделение" или "свяжитесь с поддержкой", если это не указано в контексте.
- Если в контексте нет полного ответа на вопрос, скажи об этом прямо, но НЕ дополняй ответ своей информацией.

ФОРМАТ ОТВЕТА:
- Передавай информацию из контекста дословно или близко к тексту.
- Структурируй ответ для удобства чтения (списки, абзацы).
- Будь кратким — только то, что есть в контексте.
""")

EXPENSE_PROMPT = SystemMessage(content="""
Ты финансовый консультант Сбербанка.
Тебе передают агрегированные данные по расходам клиента (категории, суммы, доли в процентах).

ТВОЯ ЗАДАЧА:
- Коротко и понятно объяснить картину расходов.
- Отметить 2–4 самых важных наблюдения (где трат особенно много / мало).
- Дать практичные советы по финансовой грамотности применительно к этим данным.

ФОРМАТ ОТВЕТА (Markdown для Telegram):
- Заголовок: одна строка с `*...*`.
- 1 блок: краткий обзор (2–3 пункта).
- 1 блок: рекомендации (3–5 конкретных советов).
- Используй маркированные списки `- ...`.
- Выделяй важные слова с помощью `*жирного*`.
- Не пиши больше ~10–12 строк.
""")

RECOMMEND_PROMPT = SystemMessage(content="""
Ты помощник Сбербанка.

Тебе передают:
- вопрос клиента
- сформированный ответ бота

ТВОЯ ЗАДАЧА:
- Определить, какие разделы сайта Сбербанка могут быть наиболее полезны клиенту после этого ответа.

ДОСТУПНЫЕ РАЗДЕЛЫ (категории):
- "cards"      — банковские карты, дебетовые/кредитные
- "deposits"   — вклады и сберегательные продукты
- "mortgage"   — ипотека и жильё
- "credits"    — потребительские кредиты, кредитные продукты
- "payments"   — платежи и переводы (ЖКХ, госуслуги и т.п.)
- "transfers"  — переводы между счетами и в другие банки
- "insurance"  — страхование
- "investments"— инвестиции и брокерские услуги

ФОРМАТ ОТВЕТА:
- Верни ТОЛЬКО JSON-массив строк категорий, без пояснений.
- Пример: ["cards", "deposits"]
- Не больше 3 категорий.
""")


# ================== КЛИЕНТ GIGACHAT ==================

giga = GigaChat(
    credentials=API_KEY,
    model="GigaChat",
    verify_ssl_certs=False,
    timeout=10
)


def classify_question(text: str) -> str:
    try:
        resp = giga.invoke([
            CLASSIFIER_PROMPT,
            HumanMessage(content=text)
        ])
        return resp.content.strip()
    except Exception:
        return "BANK"


def generate_answer(context: str, question: str) -> str:
    try:
        resp = giga.invoke([
            ANSWER_PROMPT,
            HumanMessage(content=f"Контекст:\n{context}\n\nВопрос:\n{question}")
        ])
        return resp.content
    except Exception:
        return "⚠️ Сервис временно недоступен. Попробуйте повторить запрос чуть позже."


def analyze_expenses(expenses: List[Dict]) -> str:
    """
    Анализ трат на основе агрегированных данных и GigaChat.
    Ожидается список операций с полями: date, time, description, amount, category.
    """
    if not expenses:
        return "В файле нет операций — нечего анализировать."

    total_spent = 0
    by_category: Dict[str, float] = {}

    for op in expenses:
        amount = op.get("amount", 0)
        category = op.get("category", "прочее")
        # считаем только расходы (отрицательные суммы)
        if amount < 0:
            value = abs(amount)
            total_spent += value
            by_category[category] = by_category.get(category, 0) + value

    if total_spent == 0:
        return "В файле не найдено расходов (отрицательных сумм) — возможно, это не тот файл."

    # сортируем категории по убыванию трат
    top_cats: List[Tuple[str, float]] = sorted(by_category.items(), key=lambda x: x[1], reverse=True)

    # готовим агрегированные данные в виде текста-контекста для GigaChat
    lines = []
    lines.append(f"Общая сумма расходов в выборке: {total_spent:,.0f} ₽".replace(",", " "))
    lines.append("Категории (сумма, доля %):")

    for cat, amt in top_cats:
        share = amt / total_spent * 100
        lines.append(f"- {cat}: {amt:,.0f} ₽ ({share:.1f}%)".replace(",", " "))

    context = "\n".join(lines)

    try:
        resp = giga.invoke([
            EXPENSE_PROMPT,
            HumanMessage(content=context)
        ])
        return resp.content
    except Exception:
        # запасной простой текст, если анализ через GigaChat недоступен
        fallback_lines = []
        fallback_lines.append("📊 *Краткий обзор расходов*")
        fallback_lines.append("")
        fallback_lines.append(f"Общая сумма расходов: *{total_spent:,.0f} ₽*".replace(",", " "))
        fallback_lines.append("")
        fallback_lines.append("По основным категориям:")
        for cat, amt in top_cats[:5]:
            share = amt / total_spent * 100
            fallback_lines.append(f"- *{cat}*: {amt:,.0f} ₽ ({share:.1f}%)".replace(",", " "))
        fallback_lines.append("")
        fallback_lines.append("💡 Попробуйте перераспределить траты и оставить запас 10–20% дохода на сбережения.")
        return "\n".join(fallback_lines)


def get_recommendation_tags(question: str, answer: str) -> List[str]:
    """
    Определяем, какие разделы сайта Сбербанка порекомендовать пользователю.
    Возвращаем список категорий из ограниченного множества.
    """
    allowed = {
        "cards",
        "deposits",
        "mortgage",
        "credits",
        "payments",
        "transfers",
        "insurance",
        "investments",
        "support",
    }

    try:
        import json as _json

        resp = giga.invoke([
            RECOMMEND_PROMPT,
            HumanMessage(content=f"Вопрос клиента:\n{question}\n\nОтвет бота:\n{answer}")
        ])
        raw = resp.content.strip()
        data = _json.loads(raw)
        if not isinstance(data, list):
            return []

        out: List[str] = []
        for item in data:
            if not isinstance(item, str):
                continue
            key = item.strip().lower()
            if key in allowed and key not in out:
                out.append(key)
        return out[:3]
    except Exception:
        return []


